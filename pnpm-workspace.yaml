packages:
  - "blank"

allowBuilds:
  esbuild: true
  workerd: true

# Keep the generated application's two tools and this repo's YAML parser in
# one catalog. The SDK version is bumped by release fanout, not Dependabot.
catalog:
  '@types/node': ^26.1.2
  yaml: ^2.9.0
  typescript: ^6.0.3
  wrangler: ^4.118.0
