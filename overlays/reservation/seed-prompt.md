# Reservation seed prompt

Use `.mantle/overlays/reservation/seed.json` as the auth-free local first
content surface. Replace its sample request and page copy before adding a real
form. Keep dates as simple text until a scheduling model is chosen; use Staff
MCP for runtime requests only after auth is configured.
