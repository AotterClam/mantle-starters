# Intake layout

Start with a short hero, one explanation block, one compact feature block,
then the intake form. Keep the form as the main product surface.

Edit the generated `public/index.html` directly. Do not add a component
framework, branching engine, or scoring engine until the real intake needs it.
